{
  "_comment": "User-editable budget knobs. Dungeon economics (energyCost, maxRoom, maxRunsPerDay) come from config/discovered.json (generated, gitignored) — this file only holds the numbers a human chooses. Session-08: conservative first-session budget, 3 runs at 20 energy each per session-08 brief §5. Session-09: raised to 120/5 per session-09 brief's explicit stated budget ('120 energy this session... covers five runs with margin') for Task 6's five-run stage — the guard-persistence fix landed this session made maxRunsPerSession a REAL cap across process invocations for the first time, and it was still holding at the session-08 conservative value, capping the session at 3 runs before the five-run gate could be attempted. Session-11: raised to 240/12, matching the user's own confirmed REAL daily budget (session-11 brief §6: '240 energy/day dungeon budget, 12 runs at 20 energy') rather than a session-scoped guess — the guard is date-keyed, so this is now a standing calibration to reality, not a one-day bump. Confirmed directly by the user this session (not raised unilaterally) after today's 78/5 spend from sessions 09-10 blocked the session-11 brief's ask of 3 more 20-energy runs. See DECISIONS.md.",
  "forbiddenWoods": {
    "dailyEnergyBudget": 240,
    "maxRunsPerSession": 12,
    "_potionsComment": "PERMANENT as of CLAUDE.md rule 11 (2026-08-20, session 54). Every dungeon run is now a 60-energy juiced Tier-3 entry with 3 Big Heal Juices, one run at a time, so potions are economically sound on every run that happens and the old add-before / remove-after convention (sessions 24, 42, 43, 52) has nothing left to protect against. WHY THAT CONVENTION EXISTED: session 24, a plain 20-energy orchestrator run auto-loaded potions from this block because no juiced-vs-plain gate existed in code yet; the user's directive from that incident is unconditional — non-juiced runs must NEVER use potions. WHY IT IS SAFE TO LEAVE THE BLOCK IN PLACE NOW: no code path starts a non-juiced run. scripts/liveRun.ts's main() gates the auto-derive on BOTH this block and --juiced (session 42, Task 14), and scripts/orchestrator.ts no longer starts dungeon runs at all (session 54 — its dungeon arm fails closed; its resolvePotionLoadout, which gated on this block ALONE and called runOnce with no juicedStartRun, is deleted). tests/orchestrator/dungeonArmClosed.test.ts pins both halves. IF THE ORCHESTRATOR'S DUNGEON ARM IS EVER REOPENED, THE REMOVE-AFTER-USE CONVENTION COMES BACK WITH IT — or the missing --juiced gate gets built first. Absence of this block still means 0 potions loaded, full stop; that fail-safe is unchanged.",
    "potions": {
      "allowedItemId": 131,
      "maxPerRun": 3
    }
  },
  "dendren": {
    "_comment": "Session 13, Task 9's first live fishing budget: 100/5, conservative-first-session posture, session-13 comment retained below for history. Session 15: raised to 200/15 per the session-15 brief's direct instruction ('start live fishing casts early... the user reports 15 casts available today; raise the fishing session cap') — mineFishPatterns.ts (Task 11) needs real transition volume and the daily guard resets, so today's own allowance should actually be used rather than left idle at the old 5-cast conservative figure. 200 energy covers 16 tier-1 casts (12 energy each) with margin over the requested 15; config/discovered.json's maxCastsPerDay (10, non-juiced) is the GAME's own inferred cap and is untested past 5 casts/day — if the live loop hits a real server-side cap below 15, it fails closed (CLAUDE.md §5) rather than guessing past it. Original session-13 note: maxCastsPerSession set conservatively at 5 to match Task 9's staged gate (dry-run, one cast, then five) rather than the game's own maxCastsPerDay cap — same conservative-first-session posture as forbiddenWoods' original 3-run figure (session 08). Tier-1 casts (12 energy, the free tier — SPEC-fishing.md §3) are what the live loop uses unless told otherwise. [VERIFY] CONFIRMED [live, 2026-08-16/17]: this IS the same account-wide pool forbiddenWoods spends from. Both liveFishing.ts and liveRun.ts hit real-energy-floor guard trips (HTTP 400 on start_run) within ~10 minutes of each other, both against the SAME GET /offchain/player/energy reading (10, of 420 max, regenPerHour 18) — one pool, not two. dailyEnergyBudget above is this bot's own policy ceiling per game mode, layered on top of the one real account ceiling; hitting the real floor fails closed regardless of how much of either policy ceiling remains unspent. Session 21: raised to 240/20 — two independent sources agree on this number. `config/discovered.json`'s `dendren.maxCastsPerDayJuiced` was probe-captured back in session 11 (20 casts/day juiced, vs 10 non-juiced) but never wired into policy; the user independently confirmed this session that the real daily cast limit is 20 at 12 energy each (240 total). Not a fresh guess — the probe data and the user's own figure land on the same number.",
    "dailyEnergyBudget": 240,
    "maxCastsPerSession": 20
  },
  "guards": {
    "maxConsecutiveActionFailures": 3
  }
}
